{
  "name": "csc675-final",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "comment": "npm install express && npm install body-parser && npm install express-session && npm install mysql && npm install ejs && ls && mysql --user='root' --password='password' < databasemodel.sql && node app.js",
    "run": "npm install express && npm install body-parser && npm install express-session && npm install mysql && npm install ejs && ls && mysql --user='root' --password='password' < databasemodel.sql && node app.js",
    "start": "npm install express && npm install body-parser && npm install express-session && npm install mysql && npm install ejs && ls && mysql --user='root' --password='password' < databasemodel.sql && node app.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CodingWithKevin/csc675-final.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/CodingWithKevin/csc675-final/issues"
  },
  "homepage": "https://github.com/CodingWithKevin/csc675-final#readme",
  "dependencies": {
    "body-parser": "^1.19.0",
    "child_process": "^1.0.2",
    "connect-flash": "^0.1.1",
    "ejs": "^3.1.3",
    "express": "^4.17.1",
    "express-session": "^1.17.1",
    "method-override": "^3.0.0",
    "mongoose": "^5.9.24",
    "mysql": "^2.18.1",
    "nodemon": "^2.0.4",
    "passport": "^0.4.1",
    "passport-local": "^1.0.0"
  }
}
